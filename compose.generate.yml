services:
  inferno:
    build:
      context: ./
    mem_limit: 1500m
    restart: unless-stopped
    volumes:
      - ./Rakefile:/opt/inferno/Rakefile
      - ./web:/opt/inferno/web
      - ./resources:/opt/inferno/resources
      - ./_site:/opt/inferno/_site
